/* =========================================================
   Sirius Ascent — testimonials
   The testimonial track currently uses native CSS scroll-snap
   (see .testimonial-track in components.css) with no dedicated
   JS, the same "let the browser handle swipe/momentum" approach
   as the showcase carousels in portfolio.js. This module is a
   placeholder boundary for future testimonial-specific controls
   (e.g. prev/next arrows, or wiring in a real reviews API) —
   nothing to initialise yet.
   ========================================================= */

export {};
